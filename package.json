{
  "private": true,
  "workspaces": [
    "validations",
    "commander"
  ],
  "devDependencies": {
    "@astral/commander": "*",
    "@astral/commitlint-config": "^1.6.2",
    "@astral/prettier-config": "^1.6.2",
    "@astral/eslint-config-react-ts": "^1.6.2",
    "@astral/illustrations": "*",
    "webpack": "5.75.0",
    "commitlint": "^17.0.3",
    "eslint": "^8.39.0",
    "husky": "^4.3.8",
    "lint-staged": "^11.1.2",
    "prettier": "^2.6.2",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "read-dir-names": "1.0.0",
    "rimraf": "^3.0.2",
    "stylelint": "^14.5.3",
    "typescript": "^4.7.3",
    "vite": "^3.0.4",
    "vitest": "^0.31.0",
    "copyfiles": "^2.4.1",
    "jsdom": "^22.0.0",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "storybook": "start-storybook -p 6006",
    "build-storybook": "set NODE_OPTIONS=--max_old_space_size=4072 && build-storybook",
    "chromatic": "chromatic --project-token 4692158faeb9",
    "lint": "npm run lint:all --workspaces --if-present",
    "lint:types": "npm run lint:types --workspaces --if-present",
    "lint:styles": "npm run lint:styles:all --workspaces --if-present",
    "tests": "npm run tests --workspaces --if-present",
    "tests:ci": "npm run tests:ci --workspaces --if-present"
  }
}
